{
    "title": "{{ .Site.Title }}",
    "sections": [
        {{ range $index, $e := .Site.Sections -}}
        {{ if $index -}},{{end -}} {
            "title": "{{ .Title }}",
            "link": "#{{ .Title | urlize }}"
        }
        {{ end }}
    ]   
}